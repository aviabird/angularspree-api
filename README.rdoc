== README

## For setup follow these steps.##

* Install all dependencies `$ bundle install`
* Run all migrations `$ rake db:migrations`
* Load seed data `rake db:seed`
* Load sample data `rake spree_sample:load`
